# 户型动线生成器

一个无需安装依赖的浏览器端户型动线生成工具：

- 上传 PNG、JPG 或 WebP 户型图
- 点击画布绘制多条平滑动线
- 修改每条动线的名称和颜色
- 使用快捷颜色按钮快速换色
- 在线增加、拖动和删除锚点，调节曲线
- 统一设置线宽、播放速度和循环动画
- 导出 MP4、独立 HTML 动画或静态 PNG 图片

## 预览

直接打开 `index.html`，或在当前目录启动任意静态服务器：

```powershell
python -m http.server 8080
```

然后访问 `http://localhost:8080`。
